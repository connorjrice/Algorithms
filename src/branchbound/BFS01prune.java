package branchbound;

import datastructures.Queue;
import graph.structures.KnapNode;

/**
 * Breadth-First Search with Branch-and-bound pruning algorithm.
 * For 0-1 knapsack problem.
 * @author Connor
 * 248
 */
public class BFS01prune {
    
    /**
     * Knapsack 0-1
     * @param n number of items
     * @param p profit
     * @param w weight
     * @param weight how much trunk space is available in the getaway
     * @return sum of profits in optimal set
     */
    public int getMaxProfit(int n, int[] p, int[] w,  int weight) {
        int maxProfit = 0;
        int u, v; // Nodes
        Queue Q = new Queue();
        
        return maxProfit;
    }
    
}
